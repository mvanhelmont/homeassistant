- alias: TEST BUTTON ON
  trigger: 
    - platform: state
      entity_id: switch.newkaku_014e5906_a
      to: 'on'
 
  action:
    - service: script.turn_on
      entity_id: script.alexa_speak_engine
      data:
        variables:
          message: 'test script to speech'
          volume: '0.2'

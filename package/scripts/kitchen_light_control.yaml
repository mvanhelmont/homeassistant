###########################################################################################
#
# KITCHEN LIGHT CONTROL BASED ON EVENT
#
###########################################################################################

kitchen_normal_turn_on:
  alias: "Kitchen Normal Turn On"
  sequence:
    - service: light.turn_on
      data_template:
        entity_id:
          - light.kitchen_all
          - light.sink_all
        kelvin: 2800
        brightness_pct: >-
          {% if (states.binary_sensor.node_16.state) == 'on' and (states.input_boolean.scene_goodnight.state) == 'on' %} 20
          {% elif (states.binary_sensor.node_16.state) == 'on' and (states.input_boolean.scene_goodnight.state) == 'off' %} 100
          {% else %}0
          {% endif %}

###########################################################################################
#
# HALLOWEEN
#
###########################################################################################

kitchen_halloween_turn_on:
  alias: "Kitchen Halloween Turn On"
  sequence:
    - service: light.turn_on
      data_template:
        entity_id:
          - light.kitchen_all
          - light.sink_all
        kelvin: 2800
        brightness_pct: >-
          {% if (states.binary_sensor.node_16.state) == 'on' and (states.input_boolean.scene_goodnight.state) == 'off' %} 100
          {% else %}0
          {% endif %}

###########################################################################################
#
# CHRISTMAS
#
###########################################################################################

kitchen_christmas_turn_on:
  alias: "Kitchen Christmas Turn On"
  sequence:
    - service: light.turn_on
      data_template:
        entity_id: light.kitchen_all
        kelvin: 2800
        brightness_pct: >-
          {% if (states.binary_sensor.node_16.state) == 'on' and (states.input_boolean.scene_goodnight.state) == 'off' %} 70
          {% else %}0
          {% endif %}

    - service: light.turn_on
      data_template:
        entity_id: light.sink_all
        color_name: red
        brightness_pct: >-
          {% if (states.binary_sensor.node_16.state) == 'on' and (states.input_boolean.scene_goodnight.state) == 'off' %} 100
          {% else %}0
          {% endif %}

    - service: light.turn_on
      data_template:
        entity_id: light.sink_2
        color_name: green
        brightness_pct: >-
          {% if (states.binary_sensor.node_16.state) == 'on' and (states.input_boolean.scene_goodnight.state) == 'off' %} 100
          {% else %}0
          {% endif %}

###########################################################################################
#
# KINGSDAY
#
###########################################################################################

kitchen_kingsday_turn_on:
  alias: "Kitchen Kingsday Turn On"
  sequence:
    - service: light.turn_on
      data_template:
        entity_id: light.kitchen_all
        kelvin: 2800
        brightness_pct: >-
          {% if (states.binary_sensor.node_16.state) == 'on' and (states.input_boolean.scene_goodnight.state) == 'off' %} 70
          {% else %}0
          {% endif %}

    - service: light.turn_on
      data_template:
        entity_id: light.sink_1
        color_name: red
        brightness_pct: >-
          {% if (states.binary_sensor.node_16.state) == 'on' and (states.input_boolean.scene_goodnight.state) == 'off' %} 100
          {% else %}0
          {% endif %}

    - service: light.turn_on
      data_template:
        entity_id: light.sink_2
        color_temp: 153
        brightness_pct: >-
          {% if (states.binary_sensor.node_16.state) == 'on' and (states.input_boolean.scene_goodnight.state) == 'off' %} 100
          {% else %}0
          {% endif %}

    - service: light.turn_on
      data_template:
        entity_id: light.sink_3
        color_name: blue
        brightness_pct: >-
          {% if (states.binary_sensor.node_16.state) == 'on' and (states.input_boolean.scene_goodnight.state) == 'off' %} 100
          {% else %}0
          {% endif %}

###########################################################################################
#
# BIRTHDAY
#
###########################################################################################

kitchen_birthday_turn_on:
  alias: "Kitchen Birthday Turn On"
  sequence:
    - service: light.turn_on
      data_template:
        entity_id: light.kitchen_all
        kelvin: 2800
        brightness_pct: >-
          {% if (states.binary_sensor.node_16.state) == 'on' and (states.input_boolean.scene_goodnight.state) == 'off' %} 100
          {% else %}0
          {% endif %}

    - service: light.turn_on
      data_template:
        entity_id: light.sink_1
        effect: 0
        brightness_pct: >-
          {% if (states.binary_sensor.node_16.state) == 'on' and (states.input_boolean.scene_goodnight.state) == 'off' %} 100
          {% else %}0
          {% endif %}

    - delay: 00:00:01

    - service: light.turn_on
      data_template:
        entity_id: light.sink_2
        effect: 0
        brightness_pct: >-
          {% if (states.binary_sensor.node_16.state) == 'on' and (states.input_boolean.scene_goodnight.state) == 'off' %} 100
          {% else %}0
          {% endif %}

    - delay: 00:00:01

    - service: light.turn_on
      data_template:
        entity_id: light.sink_3
        effect: 0
        brightness_pct: >-
          {% if (states.binary_sensor.node_16.state) == 'on' and (states.input_boolean.scene_goodnight.state) == 'off' %} 100
          {% else %}0
          {% endif %}

###########################################################################################
#
# SENSOR TEMPLATE
#
# https://www.home-assistant.io/integrations/template/
#
###########################################################################################
---
platform: template
sensors:
  peter_work_start_time:
    friendly_name: "Start werk Peter tijd"
    value_template: "{{(as_timestamp(states.calendar.werk.attributes.start_time)) | timestamp_custom('%H%M') }}"

- alias: "Switch - Dressoir"
  trigger:
  
    platform: state
    entity_id: switch.newkaku_011fdd46_c

  action:
    service: light.turn_on
    data_template:
      entity_id: light.dressoir_all
      kelvin: 4700
      brightness_pct: "{{ 100 if is_state('switch.newkaku_011fdd46_c', 'on') else 0 }}"

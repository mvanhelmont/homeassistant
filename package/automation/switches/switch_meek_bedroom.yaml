###########################################################################################
#
# SWITCH CONTROL FOR BALCONY
#
###########################################################################################

- alias: "Switch - Meek Bedroom"
  trigger:

    platform: state
    entity_id: switch.meek_bedroom

  action:
    service: light.turn_on
    data_template:
      entity_id: light.bedroom_all
      #kelvin: 2800
      brightness_pct: >-
        {% if (states.switch.meek_bedroom.state) == 'on' and (states.switch.mqtt_scene_goodnight.state) == 'on' %} 50
        {% elif (states.switch.meek_bedroom.state) == 'on' and (states.switch.mqtt_scene_goodnight.state) == 'off' %} 100
        {% else %}0
        {% endif %}

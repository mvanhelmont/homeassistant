- alias: 'Motion Detection - Toilet On Off'
  trigger:
    platform: state
    entity_id: binary_sensor.node_20

  condition:
    - condition: state
      entity_id: input_boolean.scene_goodnight
      state: 'on'

  action:
    - service: light.turn_on
      entity_id: light.toilet_all
      data_template:
        kelvin: 4700
        brightness_pct: >-
          {% if (states.binary_sensor.node_20.state) == 'on' %} 20
          {% else %} 0
          {% endif %}

    - service_template: >-
          {% if (states.binary_sensor.node_20.state) == 'on' %} shell_command.meek_toilet_sensor_on
          {% else %} shell_command.meek_toilet_sensor_off
          {% endif %}
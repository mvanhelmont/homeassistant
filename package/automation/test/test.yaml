###########################################################################################
#
# PLAY TEST AND SEE IF THINGS WORK OR NOT
#
###########################################################################################

- alias: "TEST - trigger statement ATOM on"
  trigger:
    platform: state
    entity_id: switch.newkaku_014e5906_a
    to: "on"

  action:
    service: notify.alexa_media_peter_s_echo_dot
    data_template:
      data:
        type: announce
        method: all
      message: "test"
      target: media_player.peter_s_echo_dot

- alias: "TEST - trigger statement ATOM off"
  trigger:
    platform: state
    entity_id: switch.newkaku_014e5906_a
    to: "off"

  action:
    service: light.turn_off
    data_template:
      entity_id: light.dressoir_all
